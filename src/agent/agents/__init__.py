"""Agent definitions.

Each subpackage is one agent: a directory with ``instructions.md`` and a
``tools/`` package. Add a new agent by dropping in a new subdirectory — no
framework code changes required.
"""
